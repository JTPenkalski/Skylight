import { DarkenDirective } from './darken.directive';

describe('DarkenDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkenDirective();
    expect(directive).toBeTruthy();
  });
});
