/**
 * A selectable option in a 'select' input control.
 * Stores the display name and the internal value of an option.
 **/
export interface ISelectOption {
  name: string;
  value: string | number;
}
