import { NoSelectDirective } from './no-select.directive';

describe('NoSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSelectDirective(null!);
    expect(directive).toBeTruthy();
  });
});
